[pytest]
asyncio_mode = auto
addopts = --import-mode=importlib
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests that require network access and API keys (deselect with -m "not integration")
